# DEVELOPMENT dependencies (tests and gates).
#
# THE AGENT NEEDS NONE OF THESE. `main.py`, `utils/` and the vendored simulator
# in `cg/` use only the Python standard library plus `ctypes` for the native
# library, and that is a deliberate constraint: the agent runs in the Kaggle
# competition container, where nothing gets installed. Before importing a
# third-party package into `main.py` or `utils/`, check that the container
# already has it.
#
#   python -m pip install -r requirements-dev.txt
#   python -m pytest -q
#
# Tested on Python 3.10 through 3.12. The pins are FLOORS (>=), not anchors:
# there is nothing here to reproduce bit for bit, only old versions to avoid.

# The suite runner (986 tests).
pytest>=7.4

# Coverage: `pytest -q --cov=. --cov-report=term-missing` (see the README).
pytest-cov>=4.1

# Property-based testing: `tests/test_invariants.py` generates boards with the
# StateBuilder and checks the agent's invariants against them. Without this
# package that file does not even COLLECT, and pytest aborts the entire run
# with a collection error -- it does not skip it.
hypothesis>=6.100
