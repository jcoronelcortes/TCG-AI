# OPTIONAL dependencies of a single script: `deck/render_deck_image.py`, which
# draws the picture of the deck. Neither the agent nor the test suite needs
# them, which is why they live apart from `requirements-dev.txt` -- installing
# pandas and numpy just to run the tests would be a toll for nothing.
#
#   python -m pip install -r requirements-render.txt
#   python deck/render_deck_image.py

pillow>=9.4
numpy>=1.24
pandas>=2.0
