# --- Python ---
__pycache__/
*.py[cod]
*$py.class

# --- Entornos virtuales ---
.venv/
venv/
env/
ENV/

# --- Testing / cobertura ---
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.mypy_cache/
.ruff_cache/

# --- Datos de partidas y salida generada (se ignora el CONTENIDO) ---
# log/: logs de partida (dato local). registros/: salida de split_turns.py.
# Se conservan las carpetas via .gitkeep; se ignora todo lo demas dentro.
/log/*
!/log/.gitkeep
/registros/*
!/registros/.gitkeep

# --- Archivos del sistema operativo ---
.DS_Store
.DS_Store?
._*
Thumbs.db
desktop.ini

# --- Editores / IDE ---
.idea/
.vscode/
*.swp
*.swo
*~

# --- Temporales ---
*.log
*.tmp

# --- Empaquetado / build de Python ---
build/
dist/
*.egg-info/
.eggs/
*.egg

# --- Paquete de submission (salida de utils/empaquetar_proyecto.py) ---
/submission.tar.gz

# --- Hypothesis (base de ejemplos local) ---
.hypothesis/

# --- Jupyter ---
.ipynb_checkpoints/
# Notebooks de analisis de meta (pesados, no se versionan)
/notebook/

# --- Backups locales ---
*.bak
# Copias-ablacion de main.py para experimentos A/B locales (pesadas)
/_v_*.py
# Instantaneas de main.py previas a un cambio grande (main_pre_lana.py, ~1.2 MB)
/main_pre_*.py

# --- Entorno / secretos ---
.env
.env.*
*.local

# --- macOS extra ---
.AppleDouble
.LSOverride
.Spotlight-V100
.Trashes
.fseventsd

# --- NO ignorar (nota para mantenimiento) ---
# Las librerias nativas del simulador (cg/*.dll, cg/*.so, cg/*.dylib) y los
# datos de referencia (deck.csv, dataset/EN_Card_Data.csv, tests/fixtures/*.json)
# SI se versionan: son necesarios para ejecutar el agente y las pruebas.
# No agregar patrones *.so/*.dll/*.dylib/*.csv que los excluirian.
kaggle_token.txt
.kaggle/
dataset/Card_ID List_EN.pdf

# --- Descarga de mazos de competidores ---
# Los CSV de decks_competidores/ SI se versionan (son el corpus para el
# simulador). Lo que se ignora es el andamiaje de la descarga: el cache de
# reanudacion y los replays crudos (~4 MB cada uno).
decks_competidores/.mazos_cache.json
decks_competidores/.cache_replays/
