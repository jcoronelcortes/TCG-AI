# The night of 15 August 2026 — the ground, and a pending that measured negative

[← Documentation index](../README.md) · [the plan this executes](../night-plan-2026-08-15.md)

**Status: finished. §0 answered "todo por defecto" (all six).
Three commits on `main`, one branch NOT merged, nothing pushed.
2839 tests pass, lint clean, tree green.**

---

## 0. The night in one table

| Track | Question | Answer |
|---|---|---|
| **G** | The red test | **Not a regression.** The reading was right; the guard was pinned to a filename. `34dcde0` |
| **M** | The memory index | **Seven entries said "sin commitear" and all seven were in `main`.** Fixed, with their commits |
| **L** | Mega Lopunny / Mega Froslass, −0.90 pp | **Not the code.** 12 000 games per arm: ±0.1 pp. Its remaining losses are starvation |
| **I** | The autopsy's first worklist item | **A missed lethal is not a finding until its price is read.** `d63f078`, `660de0a` |
| **P1** | The Tera cover price (pending, 12 Aug) | **Implemented, measured, REJECTED**: 8 of 8 matchups non-positive, 8 of 8 prize deltas negative |
| **D** | The discovery worklist | 4 missed lethals → **0 survive**. 40 sterile turns explored → **1** board worth reading |

---

## 1. Track G — the red tree, and the plan's own premise was wrong

`test_the_reading_does_not_spread` failed on a clean tree: forcing
`_promoted_lethal_reply` to zero moved one decision of 157, on `registro_007`
step 98.

The flip sits inside episode 93173834 — **the same lost game vs Marnie that the
three fixes of `c841618` came from hours earlier** — so the merge was the obvious
suspect, and a worktree at `2442f27` reported the file green.

> ### ⚠️ It was green because it never ran
>
> `records/` is git-ignored. The worktree had no records at all, so the test
> skipped itself, and `1 skipped` in a summary line reads exactly like a pass.
> With the records copied in, `2442f27` **fails identically**, and so does every
> single-file revert of the merge. What put the board there was
> `utils/split_turns.py`, re-cutting the corpus onto a new episode at 00:31.
>
> **A control arm that cannot run the measurement is not a control arm.**

**What the board is.** Turn 7, three prizes to six our way. Our active is a Teal
Mask Ogerpon ex at 20 of 210 with three Grass; Myriad Leaf Shower (30 + 30 per
Energy on *both* Actives) reads 120 over their 70 HP Marnie's Impidimp — the
prize is there from the front. The retreat takes the **same** prize: one Grass
off the front, the twin comes up with four Grass and its own 150 knocks the same
body out, and the turn ends with 200 HP in front instead of 20, the wounded ex on
the bench where its Tera prevents all damage from attacks. Leaving it in front
hands two prizes to anything at all, and their Froslass drips a counter onto it
every checkup besides. **It is the line the game actually played** (steps 99–102).

So the reading is not spreading, it is working. What was wrong is the guard: it
identified a board by **filename**, which `split_turns.py` rewrites on every run.
It now identifies one by **episode and step**, and carries both read boards with
what each decided. A third board still fails it, which is the point of it. The new
board is pinned by three tests off its own fixture, so it survives the next
rotation of `records/`.

---

## 2. Track M — seven false pendings

| Memory | Said | Actually in `main` at |
|---|---|---|
| la promoción es el asiento que la búsqueda completa | sin commitear | `eb2565c` / `f858bc5` |
| la promoción apuesta el robo del turno | sin commitear | `bb7d597` / `85d75eb` |
| el precio que no es nuestro | sin commitear | `d62002a` / `25e21df` |
| el veto de su match point | sin commitear | `bb7d597` / `85d75eb` |
| Neutralization Zone es el tercer muro | sin commitear | `89a7584` / `62bf2bf` |
| la segunda ola toma la partida | sin commitear | `e29fde2` |
| el turno que remata no deja nada listo | sin commitear | `c841618` |

Verified with `git merge-base --is-ancestor`, not by reading commit titles. The
index is what the next session reads before deciding what to work on, and seven
false "pending" entries is how a night re-implements something that shipped.

---

## 3. Track L — the −0.90 pp was the list, not the code

The only thing the night of 14 August found and could not explain. The free
question first: that number was measured at `5ee49b1`, and the day after shipped
`_op_hp_for_our_ko` (`0e9fed2`), which is **a Froslass rule**.

    HEAD vs 5ee49b1, 4 lists, 3 000 seeded games per matchup (12 000 per arm)
      mega_lopunny_1                 97.0 %   delta -0.2   dprem -0.00
      mega_lopunny_2                 98.1 %   delta +0.0   dprem -0.00
      mega_lopunny_3                 97.6 %   delta +0.1   dprem +0.00
      mega_lopunny_mega_froslass_1   97.6 %   delta +0.1   dprem -0.00

**The day's code does not touch this matchup, and the matchup sits at 97–98 %.**

⚠️ The plan's own budget (400 games/matchup) has a noise floor of ±2 pp: **it
cannot see a 0.9 pp effect at all**, and the night's first row was exactly that
wasted roll. The readable answer cost 12 000 games per arm.

**And the losses that remain are hunger.** Census with a control group over 400
games (392–8): *no attacker ready anywhere* **+27.1**, *turn without attacking*
**+23.4**, *closed with END* +23.4, *no Grass in hand* +8.9 — and one missed
lethal, which turned out to hand over the game (§4). Same diagnosis the playbook
already carries for the hard matchups. **The axis that points at it is fuel, and
fuel is a list question** — the deck owner's, not the night's.

---

## 4. Track I — a missed lethal is not a finding until its price is read

`letal_perdido` ranks first in every worklist the autopsy produces, and the first
board it offered was one where **the agent was right**.

Game 275 vs `mega_lopunny_mega_froslass_1`, turn 24: Myriad reads 210 over a Mega
Lopunny ex at 180 of 330 — three prizes, from five to two. And the same 50 HP ex
stays in front, **their pile is at two, and an ex is worth exactly two**.
`PROMO_MATCH_POINT_VETO` declined it, and driving the turn through the real
simulator (`search_begin`/`search_step`) confirms the agent walks the whole line
on purpose: forcing the promotion to the loaded Ogerpon makes it attack by itself
and cash the three prizes.

Each finding now carries `entrega_la_partida`, in the agent's own arithmetic. The
first version of that reading asked what their **active** does to the body we
leave in front — the corpse, since the knockout being priced removes it. The
second question is `_promoted_lethal_reply`, which already existed.

| board | before | after |
|---|---|---|
| mega_lopunny_mega_froslass_1 t24 | ranked | hands over the game |
| comfey t32 | ranked | hands over the game |
| jellicent_lock t42 | ranked | hands over the game |
| crustle_wall_2 t18 | ranked, **and top of the list** | hands over the game (their Dwebble replies for nothing; the Mega Kangaskhan ex behind it for 200) |

**Four of four dismissed.** The test helpers were half the work: `to_observation_class`
needs complete cards and a complete player state, and without them the projection
*raised inside the detector*, which swallows it and answers with the poorer
reading — a fixture that cannot be parsed makes a detector look conservative when
it is simply blind.

---

## 5. Track P1 — the Tera cover price: implemented, measured, rejected

The pending the user wrote on 12 August (episode 92355371 step 62 vs Festival
Lead) and nothing had touched since: the Tera of a benched Teal Mask Ogerpon ex
prevents all damage from attacks, and the promotion after a knockout charged
nothing for pulling it out of that cover.

Built on branch `el-precio-del-tera-al-promover` (`7e124f2`, **not merged**):
`PROMO_TERA_COVER_PRICE = 500` behind `PROMOTE_TERA_PAYS_FOR_ITS_COVER`, charged
only on the forced promotion, small enough that it cannot reach the knockout
bonus (+20000), the doomed penalty (−6000) or the prize band (−1500). Board
rebuilt with the StateBuilder, five tests, one corpus flip read before the
snapshot was re-frozen.

**The gate, 1 000 seeded games per matchup, branch vs `main`:**

    crustle_wall_1    86.0%  delta -0.5   dprem -0.05
    ogerpon_verde_1   86.8%  delta -0.7   dprem -0.04
    festival_lead_3   94.3%  delta -0.4   dprem -0.03
    alakazam_2        95.4%  delta -0.4   dprem -0.02
    alakazam_1        97.6%  delta +0.0   dprem -0.01
    festival_lead_1   97.8%  delta -0.3   dprem -0.01
    festival_lead_2   97.8%  delta -0.2   dprem -0.02
    dragapult_1       98.8%  delta -0.1   dprem -0.01

No single row clears the noise floor — and that is not how this reads. **Eight of
eight winrate deltas are non-positive, seven of them negative, and eight of eight
prize deltas are negative.** Under a sign test that is p ≈ 0.004: the change is
not neutral, it is consistently slightly harmful, and it is exactly the shape the
corpus flip showed — trading a 210 HP ex for a 170 HP ex, both worth two prizes,
buying the cover and paying the HP.

**The pending is answered: the price is real but it is not worth what it costs.**
The branch stays for the record; nothing merges.

---

## 6. Track D — the worklist, and it is short on purpose

Sweep of five archetypes, 300 games each: `crustle_wall_1` **254–46** is the
worst by a distance (`ogerpon_verde_1` 278–22, `alakazam_1` 288–12,
`festival_lead_1` 289–11, `dragapult_1` 293–7).

Forty of its sterile turns were run through `utils/turn_explorer.py`: **38 had
nothing to do**, which is the starvation diagnosis again, and one has a line the
agent did not take.

### The one board worth reading — `crustle_wall_1_p247`, turn 8

    US (5 prizes)                        THEM (6 prizes)
    active  Dipplin 80/80 (1G)           active  Mega Kangaskhan ex 400/400
    bench   Bayleef, 3x Ogerpon ex,      bench   Dwebble 70, Dwebble 70,
            Applin                               Cornerstone Mask Ogerpon ex
    hand    Boss's Orders, Grass, Unfair Stamp, Dawn, Meowth ex, 2 Poke Pad, ...

Do the Wave is 20 x our five benched = **100 over a 70 HP Dwebble**: a prize, and
one future wall fewer. The turn closed with END and nine cards in hand.

**Diagnosed, not guessed.** The Boss's Orders scores −1, vetoed by
`gust_without_purpose`, because `_boss_reason_with_prize` is False — and it is
False because of one deliberate line in `_boss_prize_rank` (main.py ~7005):

```python
# log 86339758 step 98: Dwebble is vetoed as a gust target
# in a Crustle deck; it must not count in the Boss's prize ranking.
if AGENT_STATE.op_is_crustle_deck and _bpr_tgt.id in (Dwebble_Grass, Dwebble_Fighting):
    continue
```

So the agent is obeying a measured rule, and the open question is its scope: the
exclusion is **unconditional**, and this board is the case it was not written
for — the body in front is a 400 HP Mega Kangaskhan ex nothing of ours can dent,
so the turn is dead anyway and the gust costs a Supporter we were going to
discard. That is the morning's first question, and it is in the matchup that
loses the most games.

---

## 7. What did not happen, and why

* **P2 (Deluxe Bomb's reply half)** — not started. P1 took the block's hours and
  ended in a measurement worth having.
* **CI stays off** (Q6). The workflow still has its `push`/`pull_request`
  triggers commented out since `a9a53ea`; that was the user's decision and a
  night does not reverse it. Stated here because a workflow that exists and never
  runs reads as a gate to anyone who does not open the file.
* **`deck.csv` untouched**, as §8 requires — even though §3 is the strongest
  argument the project has produced for the fuel axis.

---

## 8. Raw logs

`log/noche-2026-08-15/`: `G1-attribution.txt`, `L1-vs-5ee49b1.txt`,
`L2-vs-5ee49b1-n3000.txt`, `L3-autopsy-froslass.txt`, `D1-autopsy-sweep.txt`,
`D2-explorer-crustle.txt`, `P1-gate-tera.txt`.
